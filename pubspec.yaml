name: murait_cli
description: A Flutter code generator CLI for GetX and BLoC architecture (with model and repository support).
version: 0.0.1
repository: https://github.com/arpitmurait/murait_cli
homepage: https://murait.com
issue_tracker: https://github.com/arpitmurait/murait_cli/issues

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  args: ^2.5.0
  process: ^5.0.5
  path: ^1.9.1

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0

executables:
  murait_cli: murait_cli
